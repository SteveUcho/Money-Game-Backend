FROM golang:1.26.3-alpine

WORKDIR /app

# Install Air (live reload) and Delve (debugger)
RUN go install github.com/air-verse/air@latest && \
    go install github.com/go-delve/delve/cmd/dlv@latest

COPY go.mod go.sum ./
RUN go mod download

COPY . .

EXPOSE 8080
EXPOSE 2345